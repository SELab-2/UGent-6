

/**
 * Routes used in the app
 */
export enum AppRoutes {
  HOME = "/",
  COURSES = "/courses",
  DASHBOARD = "/dashboard",
  COURSE = "/courses/:id",
  LOGIN = "/login",
  LOGOUT = "/logout",
  PROFILE = "/profile",
  ERROR = "/error",
  NOT_FOUND = "/not-found"
}

